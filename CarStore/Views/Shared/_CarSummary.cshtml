@model Car

<div class="col-sm-4 col-md-4 col-lg-4">
    <h3>@Model.Brand @Model.Model</h3>
    <p><img src="@Model.ImageUrl" alt="Picture Unavailable" class="img-fluid"></p>
    <h3>€@Model.Price.ToString()</h3>
    <h4>@Model.Mileage km</h4>
    <h4>@Model.Year</h4>
</div>